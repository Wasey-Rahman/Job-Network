import React from 'react';
import Navbar from '../Navbar/Navbar';

const Details = ({Feature}) => {
    // console.log(Feature);
    // const {experiences}=Feature;
    return (
        <div>
            <Navbar></Navbar>
            {/* <h3>hi:{experiences}</h3> */}
        </div>
    );
};

export default Details;